[project]
name = "practise"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Cliff Huang", email = "hzf510920@163.com" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=2.0.0",
]

[project.scripts]
practise = "practise:main"

[tool.uv.workspace]
members = [
    "ch*/",
]

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
